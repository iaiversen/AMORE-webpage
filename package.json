{
  "name": "amore-project",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "export PATH=$HOME/.local/bin:$HOME/.local/quarto/bin:$PATH && $HOME/.local/bin/quarto --version && $HOME/.local/bin/quarto render",
    "build": "PATH=$PATH:~/.local/bin:~/.local/quarto/bin quarto render"
  }
}